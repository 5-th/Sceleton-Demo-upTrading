import React from 'react'
import { render } from 'react-dom'
import ResolutionListener from './components/ResolutionListener';

    render(
        <ResolutionListener />,
        document.getElementById('root') //точка выхода в DOM
    )
